--Steve L. Nyemba: classifier implemented using Neural Networks (in python with SciKit support)
On OSX use the following command to install the dependencies:
	sudo easy_install numpy scipy scikit-learn

